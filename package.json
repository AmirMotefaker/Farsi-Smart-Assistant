{
  "name": "farsi-smart-assistant",
  "version": "3.6.0",
  "private": true,
  "description": "Dependency-free quality gates for the Farsi Smart Assistant extension.",
  "scripts": {
    "check": "node --check inline_checker.js && node --check popup.js && node --check background.js && node --check content_script.js && node --check logic.js && node --check options.js",
    "test": "node --test tests/*.test.mjs"
  }
}
